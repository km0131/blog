services:
  app:
    build: .
    ports:
      - '3001:3000'
    volumes:
      - ./:/app:z
      - ./data:/app/data:z
    environment:
      - NODE_ENV=development
    command: ["bun", "src/index.ts"]
